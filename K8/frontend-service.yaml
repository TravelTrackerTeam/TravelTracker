apiVersion: v1
kind: Service
metadata:
  name: frontend
spec:
  selector:
    app: frontend
  ports:
    - port: 80           # Exposes on port 80
      targetPort: 5173   # Container is listening on 5173
      nodePort: 30080    # Kubernetes exposes on localhost:30080
  type: NodePort
